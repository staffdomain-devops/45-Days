---
id: 01-01
phase: 1
wave: 1
title: "Project scaffold, GitHub Actions workflow YAML, requirements.txt, prompt template stub"
depends_on: []
files_modified:
  - 45_DAYS/requirements.txt
  - 45_DAYS/prompt_template.md
  - 45_DAYS/scripts/__init__.py
  - .github/workflows/campaign.yml
autonomous: true
requirements_addressed:
  - TRIG-03
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04
  - PIPE-07
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - AI-07
must_haves:
  truths:
    - "A user can run the workflow_dispatch with contact_id and contact_email inputs from the GitHub Actions UI"
    - "Project lives in 45_DAYS/ subfolder and workflow YAML sets working-directory to it"
    - "All pinned dependencies install successfully on ubuntu-latest"
    - "prompt_template.md exists with {{token.name}} placeholders and defines the JSON output schema"
    - "campaign_output.json artifact uploads with 7-day retention on workflow success"
  proofs:
    - "cat .github/workflows/campaign.yml | grep -E 'workflow_dispatch|contact_id|contact_email' returns 3+ matches"
    - "cat .github/workflows/campaign.yml | grep 'working-directory: 45_DAYS' returns 1+ match"
    - "cat 45_DAYS/requirements.txt | grep -cE 'hubspot-api-client|requests|beautifulsoup4|anthropic|tiktoken|tenacity' returns 6"
    - "cat 45_DAYS/prompt_template.md | grep -c '{{token.' returns 5 or more"
    - "cat .github/workflows/campaign.yml | grep -E 'upload-artifact|retention-days: 7' returns 2+ matches"
---

<objective>
Lay the project foundation: create the `45_DAYS/` subfolder, pin Python dependencies, write the GitHub Actions workflow YAML with all five script steps in order, create the prompt template with token placeholders and JSON output schema, and document the HubSpot custom properties that must exist before first run.

Purpose: This is the scaffolding that every subsequent plan depends on. Without the workflow YAML, the scripts cannot run. Without `requirements.txt`, the scripts have no dependencies. Without `prompt_template.md`, `generate_campaign.py` has no contract to satisfy.

Output:
- `45_DAYS/requirements.txt` — pinned dependencies (INFRA-02)
- `45_DAYS/scripts/__init__.py` — empty marker file so `45_DAYS/scripts/` is a package
- `45_DAYS/prompt_template.md` — full prompt with `{{token.name}}` placeholders + JSON output schema
- `.github/workflows/campaign.yml` — full workflow YAML with `workflow_dispatch`, 5 script steps, artifact upload
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@rebuild-prompt.md
@CLAUDE.md
@.planning/phases/01-core-pipeline-skeleton/01-SKELETON.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create project scaffold and pin Python dependencies</name>
  <files>45_DAYS/requirements.txt, 45_DAYS/scripts/__init__.py</files>
  <read_first>
    - rebuild-prompt.md (sections "ARCHITECTURE → Python Scripts" and "FILE STRUCTURE" — defines exact dependency list and folder layout)
    - .planning/REQUIREMENTS.md (INFRA-01, INFRA-02 — pinned versions)
    - CLAUDE.md (project constraints — Python 3.12)
  </read_first>
  <action>
    Create directory `45_DAYS/` at repo root. Create subdirectory `45_DAYS/scripts/`. Create empty file `45_DAYS/scripts/__init__.py` (no content).

    Create `45_DAYS/requirements.txt` with EXACTLY these six pinned dependencies, one per line, in this order:
    ```
    hubspot-api-client>=12.0.0
    requests>=2.31.0
    beautifulsoup4>=4.12.0
    anthropic>=0.30.0
    tiktoken>=0.7.0
    tenacity>=9.0.0
    ```

    No other dependencies, no version upper bounds, no comments inline. INFRA-02 names these exact six packages with these exact lower bounds — do not change any version.

    Per INFRA-01: the subfolder `45_DAYS/` is the project home. All scripts, the prompt template, and `requirements.txt` live inside it. The workflow YAML lives at the repo root in `.github/workflows/` (NOT inside `45_DAYS/`).
  </action>
  <verify>
    <automated>test -f "45_DAYS/requirements.txt" && test -f "45_DAYS/scripts/__init__.py" && test -d "45_DAYS/scripts" && grep -c -E '^(hubspot-api-client|requests|beautifulsoup4|anthropic|tiktoken|tenacity)' 45_DAYS/requirements.txt | grep -q '^6$'</automated>
  </verify>
  <acceptance_criteria>
    - Directory `45_DAYS/` exists at repo root
    - Directory `45_DAYS/scripts/` exists
    - File `45_DAYS/scripts/__init__.py` exists and is empty (0 bytes is acceptable)
    - File `45_DAYS/requirements.txt` exists
    - `45_DAYS/requirements.txt` contains the line `hubspot-api-client>=12.0.0`
    - `45_DAYS/requirements.txt` contains the line `requests>=2.31.0`
    - `45_DAYS/requirements.txt` contains the line `beautifulsoup4>=4.12.0`
    - `45_DAYS/requirements.txt` contains the line `anthropic>=0.30.0`
    - `45_DAYS/requirements.txt` contains the line `tiktoken>=0.7.0`
    - `45_DAYS/requirements.txt` contains the line `tenacity>=9.0.0`
    - `45_DAYS/requirements.txt` contains exactly 6 package lines (no extras)
  </acceptance_criteria>
  <done>requirements.txt and 45_DAYS/scripts/ scaffold exist with exactly the six pinned dependencies from INFRA-02.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write GitHub Actions workflow YAML with workflow_dispatch, 5 script steps, and artifact upload</name>
  <files>.github/workflows/campaign.yml</files>
  <read_first>
    - rebuild-prompt.md (section "GitHub Actions Workflow" and "Steps in order" — defines the exact step order, secrets list, and artifact upload contract)
    - .planning/REQUIREMENTS.md (TRIG-03, PIPE-01, PIPE-02, PIPE-03, PIPE-04, PIPE-07)
    - .planning/phases/01-core-pipeline-skeleton/01-SKELETON.md (architectural decisions table — confirms project subfolder name `45_DAYS/`)
  </read_first>
  <action>
    Create `.github/workflows/campaign.yml` with the following exact structure:

    1. `name: AI Email Campaign Generation`
    2. `on.workflow_dispatch.inputs` with TWO required string inputs:
       - `contact_id` — description "HubSpot contact numeric ID", required `true`, type `string`
       - `contact_email` — description "Contact email address", required `true`, type `string`
    3. One job named `generate_campaign` with `runs-on: ubuntu-latest` and `defaults.run.working-directory: 45_DAYS` (per INFRA-01, PIPE-01)
    4. Job-level `env:` block exposing all four secrets and the two inputs as env vars (required by every script):
       - `HUBSPOT_API_KEY: ${{ secrets.HUBSPOT_API_KEY }}`
       - `CHORUS_API_TOKEN: ${{ secrets.CHORUS_API_TOKEN }}`
       - `ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}`
       - `TEAMS_WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}`
       - `INPUT_CONTACT_ID: ${{ inputs.contact_id }}`
       - `INPUT_CONTACT_EMAIL: ${{ inputs.contact_email }}`
    5. Steps in this exact order (PIPE-02):
       a. `actions/checkout@v4`
       b. `actions/setup-python@v5` with `python-version: '3.12'` and `cache: pip` (cache path defaults to working-directory's requirements.txt)
       c. Install deps: `pip install -r requirements.txt` (runs in 45_DAYS via defaults.run.working-directory)
       d. `name: Fetch HubSpot contact data` — `run: python scripts/fetch_hubspot.py`
       e. `name: Fetch Chorus transcripts` — `run: python scripts/fetch_chorus.py`
       f. `name: Compute campaign tokens` — `run: python scripts/compute_campaign_tokens.py`
       g. `name: Generate campaign with Claude` — `run: python scripts/generate_campaign.py`
       h. `name: Write campaign to HubSpot` — `run: python scripts/write_hubspot.py`
       i. `name: Upload campaign output artifact` — `uses: actions/upload-artifact@v4` with `name: campaign-output`, `path: ${{ runner.temp }}/campaign_output.json`, `retention-days: 7`, and `if-no-files-found: error` (PIPE-04)

    Do NOT include `if: failure()` steps for DLQ artifact upload or Teams notification — those are Phase 3 (PIPE-05, PIPE-06).
    Do NOT add any tenacity-related steps — those are Phase 3.

    Use single quotes for string values in YAML where appropriate. Indent with 2 spaces. The artifact upload path uses `${{ runner.temp }}` (the workflow expression that maps to `$RUNNER_TEMP` at job runtime).
  </action>
  <verify>
    <automated>test -f ".github/workflows/campaign.yml" && grep -q "workflow_dispatch:" .github/workflows/campaign.yml && grep -q "contact_id:" .github/workflows/campaign.yml && grep -q "contact_email:" .github/workflows/campaign.yml && grep -q "working-directory: 45_DAYS" .github/workflows/campaign.yml && grep -q "actions/upload-artifact@v4" .github/workflows/campaign.yml && grep -q "retention-days: 7" .github/workflows/campaign.yml && grep -c "python scripts/" .github/workflows/campaign.yml | grep -q '^5$'</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/workflows/campaign.yml` exists
    - YAML contains `on:` block with `workflow_dispatch:` and both `contact_id` and `contact_email` defined as required string inputs
    - YAML contains `runs-on: ubuntu-latest`
    - YAML contains `working-directory: 45_DAYS` (PIPE-01, INFRA-01)
    - YAML contains the env entry `HUBSPOT_API_KEY: ${{ secrets.HUBSPOT_API_KEY }}` (PIPE-07)
    - YAML contains the env entry `CHORUS_API_TOKEN: ${{ secrets.CHORUS_API_TOKEN }}` (PIPE-07)
    - YAML contains the env entry `ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}` (PIPE-07)
    - YAML contains the env entry `TEAMS_WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}` (PIPE-07)
    - YAML contains `INPUT_CONTACT_ID:` and `INPUT_CONTACT_EMAIL:` env vars
    - YAML references all 5 Python scripts: `fetch_hubspot.py`, `fetch_chorus.py`, `compute_campaign_tokens.py`, `generate_campaign.py`, `write_hubspot.py` in that order (PIPE-02)
    - YAML contains `actions/upload-artifact@v4` with `retention-days: 7` and `path: ${{ runner.temp }}/campaign_output.json` (PIPE-04)
    - YAML sets `python-version: '3.12'`
    - `yamllint .github/workflows/campaign.yml` exits 0 OR `python -c "import yaml; yaml.safe_load(open('.github/workflows/campaign.yml'))"` succeeds (file is valid YAML)
  </acceptance_criteria>
  <done>Workflow YAML parses cleanly, defines workflow_dispatch with both required inputs, runs all 5 scripts in order from 45_DAYS/, and uploads campaign_output.json as a 7-day artifact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write prompt_template.md with token placeholders and JSON output schema</name>
  <files>45_DAYS/prompt_template.md</files>
  <read_first>
    - rebuild-prompt.md (section "PROMPT TEMPLATE" — defines the `{{token.name}}` syntax, namespace categories, and the JSON output schema)
    - .planning/REQUIREMENTS.md (AI-03, AI-07 — token namespaces and schema match contract)
    - .planning/phases/01-core-pipeline-skeleton/01-SKELETON.md (Architectural Decisions — SDR notes property and generated date property names)
  </read_first>
  <action>
    Create `45_DAYS/prompt_template.md` as the campaign brief Claude will receive. It must use `{{token.name}}` placeholders from three namespaces — `contact.*`, `crm.*`, `campaign.*` — that `generate_campaign.py` will substitute (AI-03).

    Required structure:

    1. **Header section** (Markdown H1): "Campaign Brief: Personalised 7-Email Outreach Sequence"
    2. **Contact summary** section: must reference at minimum these token placeholders (so generate_campaign.py has stable substitution targets):
       - `{{contact.firstname}}`, `{{contact.lastname}}`, `{{contact.email}}`, `{{contact.jobtitle}}`, `{{contact.company}}`, `{{contact.industry}}`, `{{contact.num_employees}}`, `{{contact.city}}`, `{{contact.country}}`, `{{contact.website}}`
    3. **Activity history** section: must contain `{{crm.full_activity_history}}` placeholder on its own line (the substituted value is the labelled blocks assembled by `generate_campaign.py` from emails + meetings + transcripts)
    4. **Campaign context** section: must reference `{{campaign.current_date}}`, `{{campaign.eofy_timing_context}}`, `{{campaign.days_to_eofy}}`
    5. **Style constraints** section (these become part of the user prompt — the system prompt enforces the same):
       - "Use Australian English spelling throughout"
       - "Do not use em dashes (—) or en dashes (–) as sentence separators — use commas, semicolons, or full stops"
       - "Return only raw JSON. Do not wrap in markdown code fences. Do not include any prose before or after the JSON object."
    6. **Output schema section** (Markdown H2: "Required JSON Output Schema"): a fenced code block (```json ... ```) containing the EXACT schema below. `write_hubspot.py` must be able to parse this schema, so every key here is load-bearing:

    ```json
    {
      "reasoning": {
        "drop_reason_classification": "...",
        "eofy_timing_context": "...",
        "meeting_evidence_check": "..."
      },
      "email_1": { "subject": "string, single line, no em/en dashes", "body": "string, multi-line plain text" },
      "email_2": { "subject": "...", "body": "..." },
      "email_3": { "subject": "...", "body": "..." },
      "email_4": { "subject": "...", "body": "..." },
      "email_5": { "subject": "...", "body": "..." },
      "email_6": { "subject": "...", "body": "..." },
      "email_7": { "subject": "...", "body": "..." },
      "sdr_call_notes": {
        "quick_brief": "string, plain text",
        "the_hook": "string, plain text",
        "call_1_day6": {
          "opening_line": "string",
          "diagnostic_questions": ["string", "string", "string"]
        },
        "call_2_day20": {
          "opening_line": "string",
          "diagnostic_questions": ["string", "string", "string"]
        },
        "call_3_day40": {
          "opening_line": "string",
          "diagnostic_questions": ["string", "string", "string"]
        }
      }
    }
    ```

    7. **Brief and content guidance** section: provide a short campaign brief (3-5 paragraphs) instructing Claude to write a 7-email cadence over 45 days targeting EOFY (end of financial year, 30 June Australia), each email under 150 words, casual but professional tone, ending each email with a clear CTA. Each email should reference specifics from `{{contact.*}}` and `{{crm.full_activity_history}}`. Include guidance that `sdr_call_notes.quick_brief` is one paragraph summarising who the contact is and why they matter, and the three call objects (`call_1_day6`, `call_2_day20`, `call_3_day40`) correspond to scheduled SDR calls on days 6, 20, and 40 of the cadence.

    The token placeholders MUST be written exactly as `{{token.name}}` with no spaces inside braces (AI-03 contract — `generate_campaign.py` will regex-match this pattern).
  </action>
  <verify>
    <automated>test -f "45_DAYS/prompt_template.md" && grep -c '{{contact\.' 45_DAYS/prompt_template.md | awk '$1 >= 10 { exit 0 } { exit 1 }' && grep -q '{{crm.full_activity_history}}' 45_DAYS/prompt_template.md && grep -q '{{campaign.current_date}}' 45_DAYS/prompt_template.md && grep -q '"email_7"' 45_DAYS/prompt_template.md && grep -q '"sdr_call_notes"' 45_DAYS/prompt_template.md && grep -q '"call_3_day40"' 45_DAYS/prompt_template.md</automated>
  </verify>
  <acceptance_criteria>
    - File `45_DAYS/prompt_template.md` exists
    - Template contains at least 10 distinct `{{contact.*}}` placeholders matching the property list in HS-01
    - Template contains exactly one `{{crm.full_activity_history}}` placeholder
    - Template contains `{{campaign.current_date}}`, `{{campaign.eofy_timing_context}}`, and `{{campaign.days_to_eofy}}` placeholders
    - Template contains the string "Australian English"
    - Template contains the string "em dash" or "em dashes"
    - Template contains the string "Return only raw JSON"
    - Template contains a fenced ```json code block defining the output schema
    - Schema contains keys `email_1`, `email_2`, `email_3`, `email_4`, `email_5`, `email_6`, `email_7` (all 7)
    - Schema contains `sdr_call_notes.quick_brief`, `sdr_call_notes.the_hook`
    - Schema contains `sdr_call_notes.call_1_day6`, `sdr_call_notes.call_2_day20`, `sdr_call_notes.call_3_day40` each with `opening_line` and `diagnostic_questions`
    - Schema contains `reasoning` object with at minimum `drop_reason_classification` key
  </acceptance_criteria>
  <done>prompt_template.md exists with all required `{{token.name}}` placeholders across `contact.*`, `crm.*`, `campaign.*` namespaces and a complete JSON output schema matching the keys that write_hubspot.py will need.</done>
</task>

</tasks>

<verification>
**Phase 1 scaffold integration check** (run after all three tasks):

1. `tree 45_DAYS/ -L 2` shows: `requirements.txt`, `prompt_template.md`, `scripts/__init__.py`
2. `tree .github/` shows `workflows/campaign.yml`
3. `python -c "import yaml; yaml.safe_load(open('.github/workflows/campaign.yml'))"` returns no error
4. `grep -E "fetch_hubspot|fetch_chorus|compute_campaign_tokens|generate_campaign|write_hubspot" .github/workflows/campaign.yml` returns 5 lines
5. The workflow can be parsed by `actionlint` if installed: `actionlint .github/workflows/campaign.yml` (best-effort — `actionlint` may not be on the runner; YAML parse is the hard gate)

The scripts themselves don't exist yet — that's Plan 02 (fetch_hubspot), Plan 03 (chorus stub + tokens), Plan 04 (generate), Plan 05 (write). The workflow YAML references them by path, which is fine: the workflow will fail at runtime until those scripts are created, but the YAML itself is structurally complete.
</verification>

<success_criteria>
- `45_DAYS/requirements.txt` exists with exactly 6 pinned dependencies matching INFRA-02
- `45_DAYS/scripts/__init__.py` exists (empty package marker)
- `45_DAYS/prompt_template.md` exists with `{{token.name}}` placeholders across all 3 namespaces and complete output schema
- `.github/workflows/campaign.yml` exists with workflow_dispatch (contact_id + contact_email), working-directory: 45_DAYS, all 4 secrets, 5 script steps in order, and artifact upload with 7-day retention
- All four files parse correctly (YAML for workflow, valid markdown for template)
- No script files exist yet — they're created in Plans 02-05
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-pipeline-skeleton/01-01-SUMMARY.md` capturing:
- The exact filenames created
- The exact pinned dependency versions (in case future plans need to know what's installed)
- The token placeholder names used in `prompt_template.md` (so generate_campaign.py knows what to substitute)
- The JSON output schema keys defined in the template (so write_hubspot.py knows what to expect)
- Note that scripts referenced in the workflow YAML are stubs (don't exist yet) — Plans 02–05 fill them in
</output>
